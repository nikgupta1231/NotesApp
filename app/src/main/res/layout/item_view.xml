<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@color/note_color">

    <TextView
        android:id="@+id/noteTv"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:fontFamily="serif"
        android:justificationMode="inter_word"
        android:lineSpacingExtra="3dp"
        android:padding="16dp"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/delBtn"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Hey! I was to develop android app with the help of latest jetpack libraries. There are the mostly used for scalability and better maintainability of android app. This library set is favouring the MVVM architecture." />

    <ImageView
        android:id="@+id/delBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/delete_btn"
        android:padding="4dp"
        android:src="@drawable/ic_delete"
        app:layout_constraintBottom_toBottomOf="@id/noteTv"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/noteTv" />
</androidx.constraintlayout.widget.ConstraintLayout>